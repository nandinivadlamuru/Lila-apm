{
  "name": "lila-apm",
  "private": true,
  "description": "Run scripts from repo root; app lives in web/",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "install-web": "npm install --prefix web"
  }
}
